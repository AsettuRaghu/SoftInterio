/**
 * Next.js Instrumentation Hook (Optional)
 * The actual log suppression is handled by suppress-logs.js
 * which is loaded via NODE_OPTIONS in package.json
 */

export async function register() {
  // Hook is registered but actual suppression happens in suppress-logs.js
}

